# Loomspan 1.0.0-beta.4 gated release readiness

## Readiness status

**NOT RELEASE-READY — external and final-release gates are pending.** Local
snapshot preparation completed successfully on 2026-09-12. No tag, push,
workflow dispatch, upload, Maven Central publication, Console publication, or
Sidecar release was performed or authorized by this record.

| Gate | Status |
| --- | --- |
| Local beta 4 snapshot preparation | PASS |
| Sidecar SC5 snapshot integration | PASS — local snapshot stage completed 2026-09-14 |
| Integration-discovered framework remediation | PASS; public atomic invocation handoff committed and verified 2026-09-14 |
| Rebuilt snapshot reinstall and Sidecar adaptation/retest | PASS; installed snapshot and complete local Sidecar gates rerun 2026-09-14 |
| Final release-commit local checks | PENDING; must run after SC5 on the final committed release tree |
| Manual Console Release validation | PENDING; validation mode must not publish |
| Manual Maven Central Release validation | PENDING; validation mode must not publish |

## Remaining work after documentation cleanup

Updated 2026-09-14. Framework feature work through PR 5.6 and local Sidecar
snapshot integration are complete. No known framework implementation defect is
identified by the current Sidecar handoff. Final release verification is pending.
This file is the framework beta 4 release checklist; completed planning reports,
phase documents, and PR 5.1–5.6 tickets have been retired.

- [ ] Commit the cleaned framework tree; record the exact
  commit and clean worktree for final verification.
- [ ] On that final tree, repeat version consistency, version-script tests,
  full `clean verify`, and nonpublishing release-profile verification. Commands
  and earlier results are retained below as historical evidence.
- [ ] Run **Console Release** and **Maven Central Release** in nonpublishing
  validation mode. Record run URLs/results and confirm publication jobs were
  skipped. Earlier local passes do not satisfy these hosted validation gates.
- [ ] After separate publication authorization, publish framework `1.0.0-beta.4`
  to Maven Central and verify resolution without overwriting an existing release.
- [ ] Record the released framework identity in the
  [Sidecar handoff](../../../../loomspan-sidecar/ai/thoughts/beta4-handoff.md).
  Sidecar then owns its released-dependency pin, final Maven/integration/image
  checks, hosted CI, and separately authorized release/image verification.

### QA and ownership retained

**F4 remains explicitly deferred to framework QA during use, and is not passed.**
Exercise real installed-framework successes/failures under default/`NEVER`,
`ONERROR`, and `ALWAYS`, nested denial/handler failure, and concurrent mixed-child
failures preserving available history and the primary facade failure. Through
Sidecar GET, check terminal outcome/history publication, whole-record TTL,
intact large results/history, and ordinary failure envelopes without stack traces.
Failures without observer callbacks must terminate without invented events.
The Sidecar handoff owns the full QA list and results; do not duplicate its tracker.

Successful public handoff is the ownership boundary. Sidecar owns pre-handoff
refusal/discard, application wiring, HTTP callers, public observation and client
lifetime. Framework tests own internal listener ordering, blocked trace
finalization, the single shutdown budget and cutoff. No new extension contract
is required to duplicate framework-internal evidence in Sidecar.

The proposed [Observation Truncation Review](../tickets/loomspan-observation-truncation-review.md)
remains separate backlog, not an established beta 4 release gate. The
[framework design lens](../framework-feature-design-lens.md), root AGENTS.md,
README and supported-surface tests retain standing guidance and API policy.

## Historical verification evidence

The records below preserve the tested revisions, dirty-tree qualifications,
commands and results. Earlier pending-work statements describe their dated
stage; the checklist above and current Sidecar handoff own remaining work.
Retired Sidecar tickets/phases and earlier review requirements are not additional
active trackers. No runtime or release checks were rerun as part of documentation cleanup.

## Source and environment

### Atomic invocation handoff remediation — 2026-09-14

Later review of the Sidecar ownership boundary superseded the earlier “SC5 found
no framework defect” disposition: the supported framework facade could not
atomically transfer a request from an external dispatch gate into framework root
ownership. Framework implementation commit `5d19c7a` adds the supported
`SkillInvocationHandoff` / `AdmittedSkillInvocation` contract, pending/executing
root lifecycle states, pre-admitted execution, captured-authentication handling,
and actual blocked-`TRACE_COMPLETED` lifecycle evidence. The framework source
checkout was clean at `bfc2764bb661a6eccad9fd120cd687e6a911e99a` before this
readiness-record update; its rebuilt snapshot is installed locally and the starter JAR SHA-256 is
`901769CACBAF7F0CD2B39845ED1D7AC3D63294D64F74CC9D411C16925620D2AA`.

| Result | Command | Evidence |
| --- | --- | --- |
| EXPECTED RED | `.\mvnw.cmd --batch-mode --no-transfer-progress -pl loomspan-spring-boot-starter '-Dtest=AtomicInvocationHandoffRedTest' test` in an isolated worktree at base `af9f665` | Test compilation failed because `SkillInvocationHandoff` and `AdmittedSkillInvocation` did not exist; the isolated worktree was removed afterward. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -pl loomspan-spring-boot-starter '-Dtest=ApplicationApiValueTest,DefaultSkillTemplateTest,FrameworkExecutionLifecycleTest,FrameworkShutdownIntegrationTest,LoomspanPublicSurfaceArchitectureTest,SupportedSurfaceIntegrationTest' test` | 68 tests passed; covers the fifteen-type API, handoff security and single-use behavior, deterministic close ownership, one-budget cutoff, real completion append, and supported-only consumption. |
| PASS | `go -C loomspan-console test ./internal/buildtool` | Bundled Java API package and handoff guidance assertions passed. |
| PASS | `python scripts/loomspan_version.py check` | Coordinated version remains `1.0.0-beta.4`. |
| PASS | `python -m unittest discover -s scripts/tests -p "test_*.py"` | 9 tests passed. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress clean verify` | Full reactor passed; 1,148 Java tests passed. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -Prelease -pl loomspan-spring-boot-starter -am -DskipTests '-Dgpg.skip=true' verify` | Nonpublishing source/Javadoc packaging passed; existing Javadoc warnings remained non-fatal. |

The rebuilt snapshot was installed locally after the framework commit; nothing
was published. Sidecar adopted the public handoff and reran its affected unit,
integration, package, image, and release-preparation gates. Sidecar observes its
HTTP caller, public observer result, and REST-client lifetime through supported
contracts and proves Sidecar-owned normal completion;
matching framework tests directly block trace finalization and exercise both
actual listener orders as the framework-owned evidence for those behaviors.

### Earlier Sidecar handoff — 2026-09-13

PR 5.4 is now committed. The reviewed framework development revision is
`385729a254261de128df491505acd8898cc0a021`; the worktree was clean before this
documentation handoff. No production Java source changed after the runtime
base recorded below. The Sidecar baseline is committed at `20d7f92`.
The developer confirmed completed framework manual acceptance checks; the
explicit Sidecar and final release gates above remain pending.

The former Sidecar alignment report recorded 74 passing focused framework tests;
that was not Sidecar integration evidence or a new snapshot installation.
The [Sidecar handoff](../../../../loomspan-sidecar/ai/thoughts/beta4-handoff.md)
now owns its remaining work and current verification baseline. Development uses
the snapshot installed by the developer.
The original preparation record below is historical evidence and remains intact.

### Original snapshot preparation — 2026-09-12

- Base framework commit: `de2fc7d1a5154407912f5dd2f35504926e33d9d2`
- Branch: `main`
- Coordinated version: `1.0.0-beta.4`
- Verification time: 2026-09-12 22:04–22:51 America/Los_Angeles
- Maven: Apache Maven 3.9.11 through the repository wrapper
- Java used by Maven: JetBrains 21.0.2
- Python: 3.13.13
- Worktree state: dirty by design. In addition to the pipeline's untracked
  research/plan artifacts, PR 5.4 changes the README, compatibility/readiness
  documentation, and the supported-surface test/fixture. It changes no
  production Java source. Therefore the installed production artifact is built
  from the runtime at the base commit, while the cumulative readiness proof is
  still an uncommitted worktree change.
- Installed starter JAR SHA-256:
  `55EF8C23EB8CEF8B68896E1C07F740FF59CD5C9AFCEA991CC3BDDA5921F198E4`

The exact framework revision prepared for Sidecar development is the runtime at
`de2fc7d1a5154407912f5dd2f35504926e33d9d2`, with this PR 5.4 worktree's
documentation and test proof. Commit the completed PR 5.4 tree before recording
SC5 evidence. Reinstall the snapshot after any framework production change and
rerun every affected Sidecar check.

## Local preparation evidence

Before the final focused pass, the updated test was run with the original
`planning_mode: false` fixture (and without planner-only fields). It failed as
expected because the first plan JSON was returned as the direct result rather
than producing the required response or any `PLAN_CREATED` event. Restoring the
planner fixture produced the passing result below.

| Result | Command | Evidence |
| --- | --- | --- |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -pl loomspan-spring-boot-starter -Dtest=SupportedSurfaceIntegrationTest test` | 1 test; YAML planner created a serialized two-task plan, invoked Java and REST leaves, synthesized their results, and exposed public plan/tool events. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -pl loomspan-spring-boot-starter -Dtest=LoomspanPublicSurfaceArchitectureTest test` | 8 tests; closed thirteen-type API and sole supported SPI remain intact. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -pl loomspan-spring-boot-starter '-Dtest=YamlSkillCatalogTests,YamlSkillCapabilityRegistrarTests,ApplicationApiValueTest,DefaultSkillCatalogTest,DefaultSkillTemplateTest,SuccessfulSkillCompletionBoundaryTest' test` | 177 tests; exact REST, public value, catalog, validation, observation, and success-credit owners passed. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -pl loomspan-spring-boot-starter '-Dtest=FrameworkExecutionLifecycleTest,FrameworkShutdownIntegrationTest' test` | 23 tests; admission, lifecycle, one-budget shutdown, and cleanup owners passed. |
| PASS | `python scripts/loomspan_version.py check` | Coordinated version reported as `1.0.0-beta.4`. |
| PASS | `python -m unittest discover -s scripts/tests -p "test_*.py"` | 9 tests passed. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress clean verify` | Full reactor build passed; 1,134 Java tests passed. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress -Prelease -pl loomspan-spring-boot-starter -am -DskipTests '-Dgpg.skip=true' verify` | Release-profile source/Javadoc packaging passed without signing or publication. Existing Javadoc warnings were non-fatal. |
| PASS | `.\mvnw.cmd --batch-mode --no-transfer-progress install` | Full reactor and 1,134 Java tests passed; parent POM and starter snapshot JAR installed in the local Maven repository. |
| PASS | `rg -n "model|prompt|thinking_level|allowed_skills|planning_mode|concurrency|max_steps|linter|output_schema|output_schema_max_retries" README.md agent-skills/loomspan-docs/references` | Exact REST-forbidden field vocabulary is present in both human and routed agent guidance. |
| PASS | `$matches = rg -n "tag .*SNAPSHOT|v1\.0\.0-beta\.4-SNAPSHOT" README.md docs/releases; if ($LASTEXITCODE -eq 1) { Write-Output 'No invalid SNAPSHOT release/tag examples found.'; exit 0 }; $matches; exit $LASTEXITCODE` | No SNAPSHOT tag or beta 4 SNAPSHOT tag example remains. |

Documentation/source comparison classification: the checked-in
`agent-skills/loomspan-docs` REST and Java API topics are **aligned** with their
named production/test anchors and remain versioned
`1.0.0-beta.4`; no content or coverage-table change was necessary.
The former root README omissions and SNAPSHOT release examples were
**documentation drift** and are corrected by PR 5.4. The separately installed
user skill is versioned `0.1.0-SNAPSHOT` and was not used for version-sensitive
claims.

## Sidecar SC5 snapshot integration gate

Status: **PASS for the local installed-snapshot stage (2026-09-14).** The tested
Sidecar base commit was `d83902713bcf84375c3f54b8d33a66e048f0aa39`
with the complete uncommitted SC5 implementation: coordinator/test changes,
container and deployment examples, scripts, documentation, Maven/workflow
release preparation, and the pipeline research/plan artifacts. The matching
framework checkout was clean at
`bfc2764bb661a6eccad9fd120cd687e6a911e99a`; the Sidecar dependency tree
resolved `ai.loomspan:loomspan-spring-boot-starter:1.0.0-beta.4` from
the developer's local Maven repository. Updating this readiness record is the
only subsequent framework worktree change.

Independent review cycle 1 then hardened the prepared Sidecar release workflow:
the verification job now has read-only repository permission, write permissions
are scoped to the publish job, and publication refuses to replace either an
existing GitHub release or an existing GHCR version tag. The focused 28-test
suite, full 60-test Sidecar build, unchanged image
`sha256:cfb57bf9dccd687af714a3faf455f3ca3034b889ad3e154a49973d4500026f13`,
image/Kubernetes verification, nonpublishing release validation, snapshot
dependency tree, and 23 matching framework lifecycle tests were rerun and passed
after that review fix. No tag, publication, workflow dispatch, or external write
was performed.

Independent review cycle 2 corrected the dequeue-race test so it now occupies
the sole worker, queues a second request, and pauses that request after dequeue
before close wins the guarded dispatch transition. The focused 10-test
coordinator suite and full 60-test Sidecar build passed with that correction;
the unchanged image above also passed the image/Kubernetes verification again.

Independent review cycle 3 found that the test still paused before the old
`begin()` and therefore did not prove the post-`begin()` gap. The developer
approved framework remediation without weakening the atomic-dispatch
requirement. Sidecar now calls the public `SkillInvocationHandoff` under its
dispatch gate and executes the returned `AdmittedSkillInvocation` after releasing
the gate. Deterministic tests cover Sidecar-close-first, handoff-first, and
framework-close-first outcomes; real Sidecar lifecycle tests prove HTTP caller,
public observer, normal accepted-work completion, and REST-client behavior.
The developer subsequently clarified that successful handoff is the sole
Sidecar-to-framework ownership boundary. Sidecar owns pre-handoff refusal,
discard/reference cleanup, actual wiring, management/async events, and normal
accepted-work completion. Framework tests exclusively establish internal
listener ordering, blocked trace finalization, the single shutdown budget, and
cutoff; they are not substitutes for Sidecar-owned evidence, and no additional
trace-finalization SPI is required solely to duplicate those proofs. The new image is
`sha256:a1f8f996d6ade53134bf15388fdaff2513898f966b936adc21db22a7790d1742`.

The tested Sidecar `git status --short` inventory was:

```text
 M .github/workflows/ci.yml
 M .gitignore
 M README.md
 M ai/thoughts/research/2026-09-13-sidecar-packaging-release.md
 M ai/thoughts/tickets/2026-09-13-sidecar-packaging-release.md
 M pom.xml
 M src/main/java/ai/loomspan/sidecar/execution/ExecutionCoordinator.java
 M src/test/java/ai/loomspan/sidecar/execution/AuthenticatedExecutionApiIntegrationTest.java
 M src/test/java/ai/loomspan/sidecar/execution/ExecutionCoordinatorTest.java
 M src/test/java/ai/loomspan/sidecar/execution/ExecutionDiagnosticsTest.java
 M src/test/java/ai/loomspan/sidecar/execution/ExecutionShutdownIntegrationTest.java
 M src/test/java/ai/loomspan/sidecar/management/ManagementEndpointIntegrationTest.java
 M src/test/java/ai/loomspan/sidecar/rest/RestHandlerLifecycleIntegrationTest.java
?? .dockerignore
?? .github/workflows/release.yml
?? Dockerfile
?? ai/thoughts/plans/
?? examples/
?? scripts/
?? src/test/java/ai/loomspan/sidecar/config/ConfigurationReferenceTest.java
?? src/test/java/ai/loomspan/sidecar/execution/TestSkillInvocationHandoff.java
?? src/test/java/ai/loomspan/sidecar/release/
?? src/test/java/ai/loomspan/sidecar/support/SidecarApplicationFixture.java
```

| Result | Command | Evidence |
| --- | --- | --- |
| EXPECTED RED | `.\mvnw.cmd -B -ntp -Dtest=ExecutionCoordinatorTest#dequeuedTaskDoesNotEnterFacadeWhenCloseWinsDispatchHandoff test` | Before the fix, the dequeued task entered `SkillTemplate.invoke` after close won the handoff. |
| PASS | `.\mvnw.cmd -B -ntp '-Dtest=ExecutionCoordinatorTest,ExecutionDiagnosticsTest,AuthenticatedExecutionApiIntegrationTest,ExecutionShutdownIntegrationTest,RestHandlerLifecycleIntegrationTest,ManagementEndpointIntegrationTest,ConfigurationReferenceTest,ReleasePreparationTest,SupportedLoomspanApiArchitectureTest' test` | 35 tests passed after review-cycle 3 remediation. This covers atomic public handoff/close, queue and reference cleanup, authenticated planner/REST callback identity, diagnostics, async close delivery, lifecycle behavior, probes, docs, release guards, and the closed framework API boundary. |
| PASS | `.\mvnw.cmd -B -ntp verify` | Full Sidecar snapshot build passed after the framework reinstall and Sidecar adaptation; 61 tests, no failures or errors, and the executable JAR was repackaged. |
| PASS | `docker build --tag loomspan-sidecar:sc5-local .` | Non-root runtime image rebuilt as `sha256:a1f8f996d6ade53134bf15388fdaff2513898f966b936adc21db22a7790d1742`. |
| PASS | `python scripts/verify-image.py --image loomspan-sidecar:sc5-local --verify-kubernetes` | The deterministic Compose planner/two-leaf flow, independently verified JWT subject/roles, diagnostics, foreign-owner isolation, long-work liveness, bounded SIGTERM, invalid-startup refusal, image user/JVM contract, and Kubernetes manifest contract passed. |
| PASS | `python scripts/prepare-release.py --validate-only --project-version 1.0.0-beta.4 --loomspan-version 1.0.0-beta.4` | Nonpublishing version/dependency/release preparation validation passed after the workflow gained least-privilege and no-overwrite guards. |
| PASS | `.\mvnw.cmd -B -ntp dependency:tree '-Dincludes=ai.loomspan:loomspan-spring-boot-starter'` | Confirmed the locally installed beta 4 snapshot dependency. |
| PASS | `.\mvnw.cmd -B -ntp -pl loomspan-spring-boot-starter '-Dtest=FrameworkExecutionLifecycleTest,FrameworkShutdownIntegrationTest' test` (framework checkout) | 30 matching-source framework-owned lifecycle tests passed at the clean remediation commit, including public admission implementation, both actual listener orders, and blocked trace-finalization cutoff. |

Review cycle 4 initially escalated because the prior ticket text appeared to
require Sidecar-local blocked-finalization and actual framework-listener-order
proof. The developer resolved the ambiguity by revising SC5 to the explicit
ownership split above. The supporting research, implementation plan, testing
plan, and this readiness record were reconciled to that decision. No production
or test-code change and no new framework SPI were required. The cycle-4 full
61-test Sidecar build, 35-test focused suite, image build/verification, release
preparation, dependency/hash checks, and 30-test framework lifecycle suite all
passed; the reconciled Sidecar-owned lifecycle subset was then rerun after the
documentation changes.

Independent review cycle 6 found that the complete image verifier hardcoded the
three documented default host ports, so the local gate could not run while an
unrelated user service legitimately occupied port 8080. Sidecar now preserves
container ports and Compose defaults while allowing explicit verifier host-port
overrides. The full 61-test Sidecar build, focused 20-test lifecycle/release/architecture
set, image build `sha256:4debed3df78261c0983e3c9e2ba10dd510054d3aafd233dfbf6fb1abc562ee4c`,
complete image/Kubernetes verification on host ports 18080/18081/19091,
nonpublishing release validation, snapshot dependency/hash checks, and matching
30-test framework lifecycle suite passed. The user's port-8080 service was not
stopped or altered. Because this review changed implementation artifacts, one
fresh independent Sidecar review remains required before the local stage is
review-complete.

Independent review cycle 8 removed a Sidecar lifecycle-test dependency on the
undocumented `execution-trace.persistence` key and framework temporary trace-file
naming. Under the developer-approved ownership split, Sidecar now proves normal
accepted completion, public observer delivery, and REST-client lifetime solely
through its supported API/configuration boundary; the matching framework tests
continue to own blocked trace finalization and cutoff. The post-fix focused
4-test lifecycle/architecture set and full 61-test Sidecar build passed. Because
this review changed implementation artifacts, a fresh independent Sidecar review
remains required before the local stage is review-complete.

SC5 review discovered and resolved the framework ownership-transfer defect
described above; the rebuilt snapshot was installed and every affected local
Sidecar gate was rerun. Sidecar's detailed executable evidence remains in its
SC5 tests, `scripts/verify-image.py`, `scripts/prepare-release.py`, and README.
This record does not claim released-dependency resolution, hosted CI, tagging,
registry publication, release-asset upload, or published-image verification;
those gates remain pending below.

## Sidecar completion audit follow-up — 2026-09-14

The developer authorized the Sidecar audit fixes except **F4**, explicitly
deferring additional real diagnostic-mode and nested/mixed-failure integration
tests to framework QA during use. Prior local PASS statements must not be read
as proof of that full matrix. Existing Sidecar diagnostic unit tests and
successful packaged ALWAYS events remain the available evidence; the extra
matrix is neither run nor claimed passed by this follow-up.

Tested Sidecar base: `96c04bde7966d74ad2b0bfff413373eef2978bd6`, plus uncommitted
audit fixes in `ApiExceptionHandler`, `GenericRestSkillHandler`, `RestTargetClients`,
their HTTP/REST tests, and README/ticket/phase/handoff documents. Framework source
remains `bfc2764bb661a6eccad9fd120cd687e6a911e99a` with the developer-installed
snapshot. No framework implementation change or reinstall was needed; this
readiness file already had local edits, which are preserved.

The fixes prevent cross-caller cookie replay, preserve literal query plus signs,
retain MVC 400/405 problem statuses, abort unread responses without draining,
and avoid treating socket timeouts as thread interruption. Exact local evidence
and scoped self-review live in Sidecar:

- [Sidecar remaining work and verification baseline](../../../../loomspan-sidecar/ai/thoughts/beta4-handoff.md)

| Result | Command (Sidecar checkout) | Evidence |
| --- | --- | --- |
| PASS | `.\mvnw.cmd -B -ntp '-Dtest=GenericRestSkillHandlerTest,AuthenticatedExecutionApiIntegrationTest' test` | 18 tests after red regression proof. |
| PASS | `.\mvnw.cmd -B -ntp verify` | 67 tests; no failures/errors/skips; packaged JAR. |
| PASS | `docker build --tag loomspan-sidecar:audit-fixes .` | Image `sha256:64d9210532bf1ee6986d1c2524efa643a25a1631eeec5030a5e18cf791b918ca`. |
| PASS | `python scripts/verify-image.py --image loomspan-sidecar:audit-fixes --verify-kubernetes --api-port 28080 --host-port 28081 --management-port 29091` | Local packaged flow/probes/invalid startup/idle SIGTERM and static Kubernetes checks; resources cleaned up. |
| PASS | `python scripts/prepare-release.py --validate-only --project-version 1.0.0-beta.4 --loomspan-version 1.0.0-beta.4` | Nonpublishing validation only. |

No framework tests were rerun for this Sidecar-only correction. No independent
fresh-context review, hosted CI, released-dependency build, tag, or publication
is claimed. Other specific evidence limits remain in the current Sidecar handoff.
The final release gates above remain pending.

## Optional nonblocking review

- Confirm the public beta 4 compatibility note is understandable to an
  application upgrading from beta 3.
